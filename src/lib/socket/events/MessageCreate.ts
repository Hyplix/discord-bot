import { Client } from "../../client/Client";
import { Socket } from "../Socket";

export default {
    name: "MESSAGE_CREATE",
    exec: (client: Client, socket: Socket, data: {}) => {
        
    },
};